Now let me create a scorecard table:

## Agent Accuracy Summary

### Overall Score: **56% (5/9 good)**

| Category | Count | % |
|---|---|---|
| ✓ **PASS** (correct answer, high confidence) | 3 | 33% |
| ⚠ **PARTIAL** (correct direction, incomplete) | 4 | 44% |
| ✗ **FAIL** (wrong answer or no retrieval) | 2 | 22% |

---

## Detailed Results

### ✓ PASS (3/9)

| Q# | Query | Answer | Citations | Issue |
|---|---|---|---|---|
| **Q1** | Where is `planner_node` defined? | Correct file + lines 266-323 | 10/10 valid | None — high quality |
| **Q4** | What happens on `POST /agent/query`? | Correct route handler + behavior | 10/10 valid | Missing request/response field details |
| **Q7** | List CLI commands | Complete command list with locations | 10/10 valid | Line numbers missing (shows `?` instead of line) |

---

### ⚠ PARTIAL (4/9)

| Q# | Query | What worked | What failed | Root cause |
|---|---|---|---|---|
| **Q2** | Who calls `planner_node`? | Found callers | No line numbers (shows `?`) | `find_callers` returned file-level refs only; need line-level edges |
| **Q5** | How does hybrid retriever rank? | Found correct file | No ranking formula in context | Lexical FTS returned the file but not the method body |
| **Q6** | Agent nodes + what they do? | Listed 4 nodes correctly | No implementations | Retrieved only node names; bodies missing from context window |
| **Q9** | How does auth work? | Found auth middleware + buckets | No end-to-end flow | Got pieces but not the full request→auth→rate-limit chain |

---

### ✗ FAIL (2/9)

| Q# | Query | What happened | Root cause |
|---|---|---|---|
| **Q3** | What HTTP endpoints? | **0 citations** — gave up | `list_routes()` returned 0 results; routes not indexed in the graph |
| **Q8** | Index process start-to-finish? | Described agent, not indexing | `search_code` returned agent graph code instead of indexer code; `IndexPipeline` not searchable or not indexed |

---

## Root Causes & Gaps

### 1. **Routes not indexed (Q3 FAIL)**
- Command: `list_routes()` returned 0 results
- Expected: 18+ FastAPI routes in `cce/server/routes/agent.py` + `cce/server/routes/index.py`
- Why: Routes likely in Python, but framework extractor may have missed them or routes aren't decorated with `@app.get/@router.post`
- **Action:** Check if FastAPI routes use non-standard decorators or if they're inside a factory function

### 2. **Symbol bodies not in retrieval (Q5, Q6 PARTIAL)**
- `get_file_slice()` works but doesn't auto-populate context
- Planner retrieves 20–21 items but hits "has_symbol_body" coverage gap repeatedly
- **Action:** Planner needs to explicitly call `get_file_slice()` when "body" is missing instead of just returning what it has

### 3. **Line numbers lost (Q2, Q7 PARTIAL)**
- `find_callers()` returns file-level `RawEdge` with `line=0` instead of actual line
- Node locations printed as `cce/cli.py:?` instead of `cce/cli.py:79`
- **Action:** Check if `line_start` is being written to the graph correctly for all symbol types

### 4. **Indexing pipeline not searchable (Q8 FAIL)**
- Agent could not find `IndexPipeline` or `run()` method details
- But `_build_repo_context()` in `llm.py` references `.cce/index.json` 
- **Action:** `search_code("IndexPipeline")` should return `cce/indexer.py`, but it returned agent code instead — vector search may have been off-topic or `grep_code` not used

---

## Recommendations (Priority Order)

| Priority | Issue | Fix | Impact |
|---|---|---|---|
| 🔴 **P0** | Q3 FAIL: Routes @ 0 | Reindex with `--layers framework` and verify FastAPI routes are extracted | 11% accuracy → 22% |
| 🔴 **P0** | Q8 FAIL: IndexPipeline invisible | Add `IndexPipeline.run()` to symbol table (check if it's a method-of-class) | 11% accuracy → 22% |
| 🟠 **P1** | Q2, Q7: Line numbers `?` | Verify `line_start` written to DB for all symbol types; test `find_callers(line=...)` | 22% accuracy → 33% |
| 🟠 **P1** | Q5, Q6: Bodies missing | Planner should call `get_file_slice()` when body is needed; increase k for symbol retrieval | 44% accuracy → 56% |

---

## Before/After Index Stats

**This run:**
- 953 symbols indexed (vs. expected ~1200+ for this repo)
- 39 edges indexed (vs. expected 500+)
- 4 CALLS_API edges created by linker ✓

**Likely issues:**
- Routes extracted: 0 (should be ~18)
- Framework extraction may have been incomplete or skipped certain patterns

---

## What's working well ✓
1. **Citation accuracy** — all 10 citations per query are valid (no hallucination)
2. **Python symbol resolution** — Python CALLS/IMPORTS/REFERENCES working
3. **File-level retrieval** — can find the right file, just missing line numbers
4. **LLM tool-calling** — planner correctly identifies when to retry and which tools to use

## What needs work ⚠️
1. Routes not indexed → Q3 fail
2. Bodies not in context window → partial answers on implementation questions
3. Line numbers lost → citations at file level instead of exact locations
4. IndexPipeline not retrievable → can't answer indexing questions
